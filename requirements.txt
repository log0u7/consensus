# Runtime dependencies. Exact pins (==) for supply-chain reproducibility;
# bump deliberately, run `make check` and the CI security job after each bump.
httpx==0.28.1
textual==8.2.7
fastapi==0.138.1
uvicorn[standard]==0.49.0
# Explicit pin: transitive via httpx/starlette; 4.14.1 has CVE-2026-64847/63349.
anyio==4.14.2
psycopg[binary]==3.3.4
pgvector==0.4.2
py7zr==1.1.3
pydantic==2.13.4
aiolimiter==1.2.1
tenacity==9.1.4
json-repair==0.61.0
pyyaml==6.0.3
# Optional: sqlite-vec RAG backend (RAG_BACKEND=sqlite; single file, no server).
sqlite-vec==0.1.6
